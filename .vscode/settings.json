{
  "//": "Tailwind v4 at-rules (@theme, @utility, @apply) are unknown to VS Code's built-in CSS parser.",
  "css.lint.unknownAtRules": "ignore",
  "files.associations": {
    "*.css": "tailwindcss"
  }
}
